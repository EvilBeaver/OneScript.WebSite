
&Деталька(Значение = "server.port", ЗначениеПоУмолчанию = 3030)
Перем ПортСервера;

&Пластилин
Перем КаталогиДанных;

&Пластилин(Значение = "Обработчик", Тип = "Массив")
Перем Обработчики;

Перем Сервер;

Процедура Старт() Экспорт
	
	Сервер = Новый ВебСервер(ПортСервера);
	Сервер.УстановитьКаталогСервера(КаталогиДанных.КорневойКаталог());
	Сервер.УстановитьКорневойПуть(КаталогиДанных.КаталогЗагрузок());
	Для Каждого Обработчик Из Обработчики Цикл
		Сервер.ДобавитьОбработчикЗапросов(Обработчик, "Обработать");
	КонецЦикла;

	Сервер.Запустить();

КонецПроцедуры

&Желудь
Процедура ПриСозданииОбъекта()

КонецПроцедуры