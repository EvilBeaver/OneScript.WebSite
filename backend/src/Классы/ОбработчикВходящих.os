// BSLLS:IfElseIfEndsWithElse-off
&Желудь
&Прозвище("Обработчик")
&Порядок(1)
Процедура ПриСозданииОбъекта()
	
КонецПроцедуры

Процедура Обработать(Контекст, СледующийОбработчик) Экспорт
	
	Путь = Контекст.Запрос.Путь;
	Если СтрНачинаетсяС(Путь, "/downloads") Тогда
		Контекст.Данные.Вставить("Скачивание", Истина);
	ИначеЕсли СтрНачинаетсяС(Путь, "/api") Тогда
		Контекст.Данные.Вставить("API", Истина);
	КонецЕсли;
	
	СледующийОбработчик.Вызвать(Контекст);

КонецПроцедуры